# Debian installation

During the installation untick graphical desktop and standard tools

## install sudo
```sh
su -
apt install sudo
adduser USERNAME sudo
logout
sudo apt update
sudo apt upgrade
```