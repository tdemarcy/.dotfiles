# Hide the mouse

```sh
sudo git clone https://github.com/eazar001/mousetrap.git /usr/local/src/mousetrap
sudo apt install python3-setuptools
cd /usr/local/src/mousetrap
sudo python setup.py install
```